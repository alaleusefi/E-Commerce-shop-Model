@{Layout ="~/Views/Layouts/Layout.cshtml";}
@using WorldRemit.Model
@model List<Book>
<h2>Products list</h2>
<div class="top-buttons">
    <button>
        @Html.ActionLink("Add", "Add")
    </button>
</div>
<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Price</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var book in Model)
        {
            <tr>
                <td>@book.ID</td>
                <td>@book.Name</td>
                <td>@book.Price</td>
                <td>
                    @*<a>Edit</a>*@
                    @Html.ActionLink("Edit", "Edit", new { ID = book.ID })
                </td>
                <td style="padding:5px;border-bottom: 1px solid lightgrey;">
                    @*<a>Delete</a>*@
                    @Html.ActionLink("Delete", "Delete", new { ID = book.ID })
                </td>
            </tr>

        }
    </tbody>
</table>